import { createContext, useReducer } from "react";

const defaultState = { sidebarVisible: false, subMenuVisible: false };

const HeaderContext = createContext(undefined);

export function HeaderStateProvider({ children }) {
  const [];
}
